name: CI - Sales Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:

# AJOUT IMPORTANT : Permissions pour permettre l'écriture du package
permissions:
  contents: read
  packages: write

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -U pip
          pip install -e .[dev]

      - name: Run CI tests (Spark local)
        run: |
          pytest tests_ci -v

      - name: Build package
        run: |
          python -m build

      - name: Install Twine
        run: pip install twine

      - name: Publish to GitHub Packages
        env:
          # SUR GITHUB PACKAGES, LE USER EST L'ACTEUR ET LE MDP EST LE TOKEN AUTOMATIQUE
          TWINE_USERNAME: ${{ github.actor }}
          TWINE_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          twine upload \
            --repository-url https://pypi.pkg.github.com/hugoboulengeraubay \
            dist/*